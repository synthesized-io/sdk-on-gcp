actions:
  - name: kubectl smoke test
    bashTest:
      script: kubectl version
      expect:
        exitCode:
          equals: 0
  - name: Wait for deployment get ready
    bashTest:
      script: |
        kubectl wait --for=condition=Available=True --timeout=120s --namespace $NAMESPACE deploy sdk-jupyter
      expect:
        stdout:
          contains: condition met
