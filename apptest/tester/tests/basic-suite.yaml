actions:
  - name: Wait for deployment get ready
    bashTest:
      script: kubectl wait --for=condition=Available=True --timeout=180s --namespace $NAMESPACE deploy "${APP_INSTANCE_NAME}-sdk"
      expect:
        exitCode:
          equals: 0
