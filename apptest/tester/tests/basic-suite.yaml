actions:
  - name: Wait for the webserver get ready
    bashTest:
      script: |-
        curl http://$APP_INSTANCE_NAME-sdk.$NAMESPACE.svc.cluster.local
      expect:
        exitCode:
          equals: 0
