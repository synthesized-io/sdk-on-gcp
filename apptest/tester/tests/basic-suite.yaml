actions:
  - name: Wait for the webserver get ready
    bashTest:
      script: |-
        curl http://$APP_INSTANCE_NAME-service.$NAMESPACE.svc.cluster.local:8888/api
      expect:
        exitCode:
          equals: 0
